@model List<SearchProduct_Result>

@{

    <div class="row">
        @for (int i = 0; i < Model.Count; i++)
        {
            <div class="product_single col-md-4 col-sm-6 ">

                @Html.Partial("_ProductItemSearch", Model[i])
            </div>

        }
    </div>

}